{
  "name": "kendo-vanilla-ui",
  "version": "0.0.1",
  "description": "Kendo UI Vanilla JavaScript UI Components",
  "main": "src/index.js",
  "scripts": {
    "dev": "cd playground && npx vite",
    "build": "rm -rf dist && npx tsc",
    "bundle": "rm -rf dist/bundle && npx esbuild src/index.ts --bundle --minify --sourcemap  --outfile=dist/bundle/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "kendo",
    "vanilla",
    "javascript",
    "js"
  ],
  "author": "Progress",
  "license": "ISC",
  "devDependencies": {
    "@progress/kendo-theme-default": "^5.10.0",
    "lighterhtml": "^4.2.0",
    "tslib": "^2.4.1",
    "typescript": "^4.8.4",
    "vite": "^3.2.2"
  }
}
